# Terraform state files - never commit these (contain sensitive infrastructure data)
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
*.tfstate
*.tfstate.backup

# Terraform working directory
terraform/.terraform/

# Python virtual environment
venv/
